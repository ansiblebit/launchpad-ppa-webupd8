---
# file: launchpad-ppa-webupd8/tests/boxes.yml
#

vagrant:


  # Debian

  jessie64.vagrant.dev:
    enabled: true
    box: debian/jessie64
    network:
      name: private_network
      ip: 192.168.122.1

    provider:
      virtualbox:
        memory: 1024
        cpus: 1


  # Ubuntu

  precise64.vagrant.dev:
    enabled: true
    box: ubuntu/precise64
    network:
      name: private_network
      ip: 192.168.124.2

    provider:
      virtualbox:
        memory: 1024
        cpus: 1

  trusty64.vagrant.dev:
    enabled: true
    box: ubuntu/trusty64
    network:
      name: private_network
      ip: 192.168.124.4

    provider:
      virtualbox:
        memory: 1024
        cpus: 1

  vivid64.vagrant.dev:
    enabled: true
    box: ubuntu/vivid64
    network:
      name: private_network
      ip: 192.168.124.6

    provider:
      virtualbox:
        memory: 1024
        cpus: 1

  wily64.vagrant.dev:
    enabled: true
    box: ubuntu/vivid64
    network:
      name: private_network
      ip: 192.168.124.8

    provider:
      virtualbox:
        memory: 1024
        cpus: 1

  xenial64.vagrant.dev:
    enabled: true
    box: ubuntu/xenial64
    network:
      name: private_network
      ip: 192.168.124.10

    provider:
      virtualbox:
        memory: 1024
        cpus: 1

  yakkety64.vagrant.dev:
    enabled: true
    box: ubuntu/yakkety64
    network:
      name: private_network
      ip: 192.168.124.12

    provider:
      virtualbox:
        memory: 1024
        cpus: 1
